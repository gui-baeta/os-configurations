{
  ...
}:
{
  #
  # NOTE Configure This. May be with Home Manager
  # Artifacts in Chromium
  # If you see artifacts in Chromium, forcing the vulkan-based backend
  # might help.
  # Go to `chrome://flags` and enable `#ignore-gpu-blocklist` and `#enable-vulkan`.
  # https://wiki.archlinux.org/title/AMDGPU#Artifacts_in_Chromium
}
